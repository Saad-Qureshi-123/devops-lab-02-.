name: Node.js CD

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Deploying
      run: |
        echo "Deploying with secret key..."
        echo "Key: ${{ secrets.DEPLOY_KEY }}"